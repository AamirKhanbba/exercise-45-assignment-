const makeShirt = (size: string, message:string): void => {
    console.log(`The shirt is ${size} size and has the message: "${message}".`);

}

// calling function
makeShirt("madium", "keep calm and code on!");